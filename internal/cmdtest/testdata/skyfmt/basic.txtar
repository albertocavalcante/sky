# Test basic skyfmt functionality

# Check mode - unformatted file should exit non-zero
! exec skyfmt -check unformatted.star
stdout 'unformatted.star'

# Check mode - formatted file should exit zero
exec skyfmt -check formatted.star

# Diff mode should show changes
exec skyfmt -d unformatted.star
stdout '@@'

-- unformatted.star --
def foo(  x,y ):
  return x+y

-- formatted.star --
def foo(x, y):
    return x + y
